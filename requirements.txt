# ===== GPU SETUP INSTRUCTIONS =====
# For GPU acceleration (NVIDIA):
# 1. Install CUDA Toolkit: https://developer.nvidia.com/cuda-downloads (version 12.1 or 11.8)
# 2. Install PyTorch with GPU support:
#    For CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#    For CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 3. Verify GPU: python -c "import torch; print(torch.cuda.is_available())"
# ===================================

# --- Web & API framework ---
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.3

# --- Audio processing, tagging & stem separation ---
yt-dlp>=2025.10.22
mutagen>=1.47.0
moviepy>=2.1.1
demucs>=4.0.1  # htdemucs_ft model provides best quality stem separation
# GPU Support: Install PyTorch with CUDA 12.1 support using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or for CUDA 11.8: --index-url https://download.pytorch.org/whl/cu118
torch>=2.5.1  # GPU-enabled version (install separately with command above)
torchaudio>=2.5.1  # Audio processing with GPU support

# --- Video rendering and manipulation ---
imageio>=2.37.0
imageio-ffmpeg>=0.6.0
numpy>=2.3.4
Pillow>=10.4.0  #  supports Python 3.14+
decorator>=5.1.1
proglog>=0.1.10

# --- YouTube integration ---
google-api-python-client>=2.150.0
google-auth>=2.36.0
google-auth-oauthlib>=1.2.1

# --- Spotify integration ---
spotipy>=2.24.0

# --- Terminal progress and environment variables ---
tqdm>=4.67.1
python-dotenv>=1.0.1

# --- EC2 Upload (SSH/SCP) ---
paramiko>=3.5.0
scp>=0.14.5

# --- Extra utilities ---
requests>=2.32.3
pydantic>=2.10.3
youtube-search-python>=1.6.6
beautifulsoup4>=4.12.3
google>=3.0.0
playwright>=1.49.0
celery>=5.3.6
redis>=5.0.3
watchdog>=4.0.0
pytz>=2024.2
pydub>=0.25.1

# --- Fuzzy matching for song identification (99%+ accuracy) ---
rapidfuzz>=3.0.0

# --- TikTok Integration ---
TikTokApi>=1.0.0
requests-toolbelt>=1.0.0
